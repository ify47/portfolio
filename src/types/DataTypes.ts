export type NavigationType = {
  name: string;
  scroll: string;
};

export type SkillsType = {
  name: string;
  location: string;
  alt: string;
};

export type ProjectListTypes = {
  image: string;
  alt: string;
  name: string;
  description: string;
  tools: string[];
  site: string;
  github: string;
  style: boolean;
};
